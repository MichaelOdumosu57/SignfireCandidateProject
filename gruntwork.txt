add to journal.log later
https://www.competa.com/blog/how-to-run-npm-without-sudo/

install path /usr/node_modules

npm config set prefix ~/npm

/usr/lib
├── @angular/cli@7.3.7
├── npm@6.9.0
└── pnpm@3.0.1

/home/uoul/npm/lib
└── (empty)

npm install -g npm
npm install -g @angular/cli

success

to genenrate a service

ng

if something is to respond to an action in another component, the changing property should be placed in the service for better results

to incrase the file watch capabilities of Angular
sudo sysctl fs.inotify.max_user_watches=524288
sudo sysctl -p --system


to access the DOM from angular
https://stackoverflow.com/questions/50169543/how-can-i-access-dom-elements-in-angular


to zip your computer
 zip -rv My_Computer.zip My_Computer -x "*/\node_modules/*" 